# Decisions Registry

One line per decision. Format:
- Pointer: `- **YYYY-MM-DD** · <slug> · <one-line essence> → \`openspec/changes/archive/<dir>/\``
- Inline (no archive): `- **YYYY-MM-DD** · <slug> · [inline] <short rationale>`

---
Older entries: `knowledge/decisions/HISTORY.md` (rolled, load on demand).

- **2026-07-13** · checks.py-universal-builtins · `test-quality` and `data-scale` detectors ship as first-class in-process AST builtins in `scripts/checks.py` (family=check), not per-repo `checks/*.py` — deciding fact: per-repo check dirs don't propagate via `sync_scaffold.py`, so only checks.py builtins are universal → `openspec/changes/archive/2026-07-13-defect-prevention-detectors/`
- **2026-07-13** · defect-detectors-advisory-default · both detectors enabled by default (autodetect) because their findings are advisory-only (audit report, operator-triaged, no CI-gate impact) — low-FP core rules plus advisory enforcement make default-on FP-safe; a noisy repo disables the whole detector via `[checks.<name>].enabled = false` → `openspec/changes/archive/2026-07-13-defect-prevention-detectors/`
- **2026-07-13** · per-rule-toggle-v2-deferral · two noise-prone test-quality rules (`unfrozen-clock`, `discarded-return-flag`) ship enabled but advisory-labeled; a per-rule `disabled_rules` config toggle (so high-signal rules stay on while noisy ones are silenced) is deferred to v2, observed from downstream adoption noise first → `openspec/changes/archive/2026-07-13-defect-prevention-detectors/`
- **2026-07-13** · delegation-wrapper · post-processing-only ("ingest") wrapper mechanizes the eight delegation sites' hand-rolled `opencode run` post-processing (fallback detection, text extraction, status classification, verdict capture, marker assertion) into one tested script plus an untracked JSONL telemetry ledger per run; the pivotal design call: NOT a full-invocation wrapper — every literal `timeout … opencode run … < /dev/null` invocation stays intact so the budget-agreement and delegation-safety guards keep scanning them → `openspec/changes/archive/2026-07-13-delegation-wrapper-telemetry/`
- **2026-07-14** · apply-throughput-resume · green-path targeted-test cadence (same test tool, scope-narrowed per task; full suite once before completion) + four-point fresh-executor resume contract (skip `[x]`, resume at first `[ ]`, reconcile in-flight, carry distilled state); scope-narrowing the same tool is NOT an improvised command → `openspec/changes/archive/2026-07-14-apply-throughput-resume/`
- **2026-07-14** · spec-delta-structure-detector · a `checks.py` in-process builtin (NOT scaffold_lint, NOT a standalone script) discovers changes by directory presence and structurally validates delta specs, because `openspec validate` is proposal.md-gated and blind to MEDIUM changes' deltas; advisory at audit level, enforcing at verify time (T4) — closes ratchet `medium-change-spec-delta-unvalidated` via the `check` disposition; a checks.py builtin propagates for free via the existing manifest entry → `openspec/changes/archive/2026-07-14-skill-debloat-gates/`
- **2026-07-14** · model-id-agreement-lint · a `scaffold_lint.py` check (golden-source-only, like budget-agreement) asserting every `deepseek[-/]v4` token in the instruction surface matches the sanctioned delegation-harness §(f) table by exact-string membership; scaffold_lint stays non-manifest because it polices the scaffold's own prompt surface → `openspec/changes/archive/2026-07-14-skill-debloat-gates/`
- **2026-07-14** · verify-adversarial-fixtures · self-review MUST author its own adversarial/boundary fixtures for logic-bearing diffs (canonical: config.yaml rules.verify; skill = operational cite; no spec delta) → `openspec/changes/archive/2026-07-14-verify-adversarial-fixtures/`
- **2026-07-14** · knowledge-surface-bounding-2 · status_lint C3 exempt-section word budgets + new boot_surface_lint aggregate boot-byte gate; no spec delta (mechanizes canonical AGENTS.md prose) → `openspec/changes/archive/2026-07-14-knowledge-surface-bounding-2/`
- **2026-07-14** · delegated-context-caching · var-paths-last delegated-prompt reshape + batch-AGENTS.md convention; OPENCODE_DISABLE_PROJECT_CONFIG proven to couple AGENTS.md-injection with agent-discovery (B deferred) → `openspec/changes/archive/2026-07-14-delegated-context-caching/`
- **2026-07-14** · correctness-audit-meta-hardening · liveness Active-item + charter status marker + wave-namespace separation, blind close-out coverage-gap review, bounded scope-seeding checklist, post-close ledger; 2 guarded knowledge_lint detectors; classes 9-12 awareness-only (claims-ledger = OW-16) → `openspec/changes/archive/2026-07-14-correctness-audit-meta-hardening/`
- **2026-07-14** · product-audit-skill · new pull-only `product-audit` capability (blind attack-list → five-lane evidence fan-out → per-attack disposition → operator ratification → `PRODUCT:` verdict); claims-ledger convention with a content-sha256 covered-file manifest (git-independent, robust to checkout mtime resets); reused the existing finding-closure-ratchet dispositions verbatim (no ratchet spec change); single-session altitude → no liveness obligation, no liveness lint → `openspec/changes/archive/2026-07-14-product-audit-skill/`
- **2026-07-14** · archive-mechanization · new `archive-mechanization` capability (all ADDED): deterministic `scripts/apply_delta_spec.py` promotes ADDED/REMOVED/RENAMED spec-delta operations (plan-all-in-memory, write-all-or-nothing, any anomaly halts and writes nothing, MODIFIED always deferred to the LLM) plus conflict-guarded `scripts/archive_move.py`; ADDED-name-collision is no longer a silent overwrite (BREAKING contract change vs the old sync-skill prose — a strict safety improvement) → `openspec/changes/archive/2026-07-14-archive-mechanization/`
- **2026-07-14** · notes-checkpoint-detector · new `checks.py` builtin `notes-checkpoint-structure` (same registration shape as `spec-delta-structure`) enforcing the 5-field `notes.md` verify checkpoint, triggered only on a verify-due change (all `tasks.md` boxes `[x]`) so WIP/no-tasks changes are never false-flagged; ADDED requirement in `defect-prevention-detectors` → `openspec/changes/archive/2026-07-14-skill-debloat-residual/`
- **2026-07-14** · freeze-check-token · reviewer tightens its severity verdict to a strict machine-parseable `VERDICT: PASS|NEEDS REVISION` line; new `scripts/freeze_check.py` DERIVES `FREEZE: READY|BLOCKED — <reason>` from `VERDICT` (+ `PREMISE` for proposals) — freeze *policy* centralized in one deterministic, tested place while the reviewer stays decoupled from workflow policy; MODIFIED requirement in `premise-review-gate` → `openspec/changes/archive/2026-07-14-skill-debloat-residual/`
- **2026-07-14** · verify-coverage-debloat · verify's fuzzy "search codebase for keywords / assess if implementation likely exists" clauses replaced by a deterministic requirement/scenario enumeration plus a mandatory behavioral-evidence coherence note (grounded in the mandatory behavioral review, not keyword search); no spec delta — verify-skill prose only, `verify-multimodel-gate` untouched → `openspec/changes/archive/2026-07-14-skill-debloat-residual/`
- **2026-07-15** · boot-budget-per-repo · [inline] `boot_surface_lint.py` byte budget (WARN 80k / FAIL 100k) is now per-repo-overridable via a `[boot_surface_lint]` table in `checks.toml` (`warn_bytes`/`fail_bytes`), mirroring the `knowledge_lint` config pattern; absent/non-int/negative/inverted values fall back to the module defaults (scaffold itself unchanged at 80k/100k). Surfaced by psc-monitor propagation (109.6KB boot surface hard-FAILed the one-size-fits-all constant); SMALL scaffold change (plan: `plans/archive/boot-budget-per-repo.md`), flash apply + flash verify READY
- **2026-07-16** · outstanding-work-skill-split · renamed `outstanding-work-review` → `outstanding-work-scan` and narrowed it to the cheap deterministic-gather + untriaged-bucket-dedup path; added a new pull-only `outstanding-work-deep-sweep` skill for the heavy five-category residual prose sweep (parallel subagents), mirroring the repo's own deterministic-detector-vs-deep-LLM-audit separation (`checks.py`/`facts.py` vs `correctness-audit`); scaffold-rename mechanics (manifest repoint + tombstone) keep the old dir deleted downstream on sync → `openspec/changes/archive/2026-07-16-split-outstanding-work-skills/`
- **2026-07-16** · outstanding-work-deep-sweep-new-capability · the deep sweep is authored as a NEW capability spec (`outstanding-work-deep-sweep`) rather than an ADDED requirement folded into `outstanding-work-view`, mirroring how `correctness-audit`/`composition-audit`/`product-audit` each own a capability spec — keeps the deterministic-fact contract and the deep-LLM-sweep contract from blurring together in one capability → `openspec/changes/archive/2026-07-16-split-outstanding-work-skills/`
- **2026-07-17** · scaffold-lint-tombstone-derived-vocab · `scaffold_lint`'s non-openspec skill-reference scan vocabulary is now derived from `scaffold_manifest_removed.txt` tombstones instead of a hand-maintained frozenset, so a retired skill name is always flagged and the vocabulary cannot drift; adding the 4 missing current names would have been a behavioral no-op (valid names resolve from disk), so the fix targets the actual blind spot (removed names) — supersedes the `clarify-audit-tooling-surface` explicit-frozenset decision (2026-07-03) → `openspec/changes/archive/2026-07-17-reconcile-parked-backlog/`
- **2026-07-17** · tracker-sweep-is-apply-work · the parked-`knowledge/questions/` tracker sweep (deleting/trimming stale files against an adversarially re-verified evidence file) is scoped as apply work, not archive work, because the tracker content is the change's own evidence-gated deliverable and must pass verify like the rest of the diff; archive still reconciles normally afterward, adding only this change's own follow-on entries → `openspec/changes/archive/2026-07-17-reconcile-parked-backlog/`
- **2026-07-17** · archive-follow-on-not-already-resolved · the archive step must verify a candidate follow-on was not already resolved by the very change being archived — checking the change's own diff/commits, not just its notes — before filing it into `knowledge/questions/INDEX.md`; root-caused by two tracker files (`repo-lint-fetchall-docstring.md`, `data-lint-sqlite-backend.md`) filed stale by their own change's archive commit → `openspec/changes/archive/2026-07-17-reconcile-parked-backlog/`
- **2026-07-17** · handoff-lint-exempt · `knowledge_lint.py` scanned the mandated `knowledge/HANDOFF.md` mid-session handoff as steady-state prose, tripping four check families on its forward references/retired-path tokens/planned archive pointers/quoted blocks and making the mandated handoff un-committable (agents were deleting it to reach green); fixed by extending the existing `knowledge/research/` exclusion precedent to this mirror-image, forward-looking case, keyed to the exact `knowledge/HANDOFF.md` path in both `knowledge_lint.py` and `sync_scaffold.py --check-refs` → `openspec/changes/archive/2026-07-17-handoff-lint-exempt/`
- **2026-07-17** · knowledge-lint-gitignored-citation-exempt · generalized `knowledge_lint.py`'s hardcoded `output/` broken-citation exemption to any git-ignored citation target by threading the existing `is_ignored` git-check-ignore callable into `_check_broken_citations`; tracked-path drift still flags; the `output/` literal is retained as the git-unavailable fallback → `openspec/changes/archive/2026-07-17-knowledge-lint-gitignored-citation-exempt/`
- **2026-07-17** · roll-decisions-index · pressure-triggered chronological roll of the decisions registry (INDEX newest-tail ≤ budget, HISTORY on demand) chosen over threshold raises (treadmill — psc-monitor re-FAILed in 48h), topic splits (per-entry routing judgment), and calendar splits (boundary never arrives before pressure); mechanized as `knowledge_lint` `decisions-index-budget` + deterministic `roll_decisions.py`; budget raises are operator decisions → `openspec/changes/archive/2026-07-17-roll-decisions-index/`
