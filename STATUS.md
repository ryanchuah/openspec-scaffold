# Status

## Current state
Project initialised from openspec-scaffold. Delegated-work governance hardened: the apply-executor now stops and reports on non-convergence instead of looping, the reviewer has a raised budget with incremental output and partial-salvage on timeout, and a Claude `PreToolUse` commit-test-gate deterministically blocks commits when tests are not green. Instruction surface hardened: a tier-confirmation gate prevents non-fast-track agents from self-classifying and executing without operator confirmation, and six stale/hazardous instruction sites were reconciled to shipped behavior. Delegation robustness hardened: all delegated `opencode run` invocations now close stdin to prevent permission-prompt hangs, per-agent permissions contain write-capable executors, the non-convergence canary is rebuilt as a non-gameable impl-module+frozen-test, and the commit-test-gate ships a smoke fixture with documented hook-wiring procedure. Verify hardened: independent multi-model verification passes (platform-specific chains; Claude self→pro→flash, OpenCode self→flash) now run as hard gates after the orchestrator's self-review, layered before the artifact checklist, so the final release-quality gate is never decided by a single model's blind spots. The shared `opencode run` delegation harness (invocation hardening, assert-real-agent-ran, surgical-kill, EXIT-sentinel completion detection, timeout budgets) is now single-sourced in `ai-docs/delegation-harness.md`, with the four delegating skills reduced to citations plus per-phase specifics — extraction, not redesign, with all behavior and budgets preserved byte-for-byte. The verify gate is tier-scaled: multi-model passes apply to MEDIUM/COMPLEX only; a simplicity/quality gate and a conditional security gate (for sensitive-surface changes) were added, layered after the verifier passes; the archive-executor bodies were hardened to handle RENAMED spec requirements; and a new `scripts/test_executor_body_agreement.py` guard byte-compares each `.claude`/`.opencode` executor-pair body, failing when the two drift. The `ai-docs/open-questions.md` always-loaded surface is now bounded by a horizon split: it holds ONLY active items (blockers, operator-decision items, in-flight backlogs); the deferred/monitored long tail lives in `ai-docs/parked-follow-ons.md`. Instruction-surface rule-restatements single-sourced: five rule-families previously duplicated 3–5× across the instruction surface each assigned one canonical home with all other sites reduced to per-context specifics + a citation — extraction, not redesign, with every rule preserved verbatim. The canonical homes and the cite-don't-restate convention live in `ai-docs/workflow-lessons.md` §2.

## Latest change — lean-boot-context SHIPPED (2026-06-18)

State-file bounding rules shipped + enforced at archive; psc-monitor appendix relocated to on-demand
ai-docs/ files, slimming AGENTS.md from 707 to 321 lines. P3: three new state-bounding conventions
(≤150-word STATUS entries, decisions Date/Status + ≤300-word cap, open-questions parking+pointer-stub
rule) live in AGENTS.md §"State, write discipline…" and enforced at archive by both archive-executor
bodies — forward-only, no risky retroactive summarization. P2: psc-monitor's 412-line appendix
verbatim-relocated to 5 ai-docs/ files (schema, API, layout, ops-runbook, project-reference) with an
On-demand references table; every citation repointed; no load-bearing constraint lost. Sync guard
bumped 300→350 to fit lean repos after rule propagation. Verify: primary self-review found 2 defects
(test regression, P2 content loss) → fixed; pro + flash verifier passes both READY, zero defects;
all scaffold tests green; all four convergence/refs gates green. Decisions in ai-docs/decisions.md;
follow-ons split by horizon (active → open-questions.md, parked → parked-follow-ons.md). Archive:
openspec/changes/archive/2026-06-18-lean-boot-context.

## Prior change — single-source-rules SHIPPED (2026-06-17)

Instruction-surface rule-restatements single-sourced — the C2/W7 audit finding that closed the consolidation-plan ledger. Five rule-families (tasks.md = apply-only, model-assignment matrix, never-record-counts, mock-API war-story, web-research convention) were assigned one canonical home each; every other site now carries its per-context specifics + a citation, with the full rule preserved verbatim at its home. The self-enforcing convention and a five-row single-source registry live in `ai-docs/workflow-lessons.md` §2. No behavior change — all `--model` invocation lines and `model:` frontmatter fields preserved byte-for-byte; `scripts/test_executor_body_agreement.py` stays green. MEDIUM change (tasks.md only), mirrors the `dedup-scaffold` extraction-not-redesign pattern. Verify: self-review + deepseek-v4-pro + deepseek-v4-flash verifier passes all READY, zero defects; guards and `openspec validate --all` green. The Phase 2 propagation (re-sync changed managed files to extrends + psc-monitor via `sync_scaffold.py`) is pending operator go-ahead — tracked in `ai-docs/open-questions.md`. Full rationale and rejected alternatives in `ai-docs/decisions.md`; parked follow-on (config.yaml rules drift risk across repos) in `ai-docs/parked-follow-ons.md`. Archive: `openspec/changes/archive/2026-06-17-single-source-rules`.

## Prior change — split-open-questions SHIPPED (2026-06-17)

The `cap-status-log` assumption that `ai-docs/open-questions.md` could stay "read in full" bounded by the retired-notes mechanic didn't hold for high-activity repos — extrends' file stayed over the Read tool's length cap even after a conservative resolved-items prune, dominated by genuinely-open follow-ons accreting faster than items resolve. This change introduces a **horizon split**: `ai-docs/open-questions.md` now holds ONLY active items (open blockers flagged **BLOCKING**, operator-decision items, in-flight backlogs that gate other work), while the deferred/monitored/low-priority long tail lives in `ai-docs/parked-follow-ons.md` (grouped by `##` area headers, loaded on demand — NOT part of the mandatory onboarding read). A live blocker is never parked while live — the split is by horizon, never by age, preserving blocking-item visibility while bounding the always-loaded surface. AGENTS.md's MANDATORY read block was amended (parked-follow-ons.md explicitly excluded from the mandatory read), the horizon-split rule was encoded in the State/write-discipline section, and the "Authored deliverables" directory listing was updated. Both archive-executor §3c bodies now route new follow-ons by horizon at archive (active → open-questions.md, parked → parked-follow-ons.md), and the archive skill verify-checklist asserts no live blocker was parked. The physical two-file split (C2) was chosen over header-scan-only (unsound — a change-named section can hide a live blocker the scanning agent never opens) and index-only-with-pointers (delays but does not bound, breaks for ad-hoc items) — the same reasoning `cap-status-log` used for the STATUS.md↔status-log.md split: physical separation ENFORCES the bound rather than making it merely satisfiable via agent self-discipline. No delta specs — MEDIUM change, tasks.md only. Verify: READY FOR ARCHIVE — self-review + deepseek-v4-pro verifier pass both VERDICT: READY with zero defects (flash pass skipped per operator); `sync_scaffold.py --check` against both downstream repos confirmed drift set exactly this change's four managed files with every other file IDENTICAL; AGENTS.md span-merge clean; C4 guard green; `openspec validate --all` passed. Rationale and rejected alternatives in decisions.md; active follow-ons in open-questions.md (propagation backlog HIGH/BLOCKING), parked ones in parked-follow-ons.md (one-time migrations, redundant-summary cleanup).

## Immediate next action
No proactive build in flight. `lean-boot-context` shipped and committed across all three repos (scaffold aefbb79, psc-monitor 790c78d, extrends f08bdca). The new state-bounding rules are forward-only — they bind at each repo's *next* archive. Watch the next real archive to confirm the archive-executor applies the ≤150-word STATUS budget, the open-questions parking+pointer-stub, and the decisions Date/Status+≤300-word cap. The sync tail-guard (350) is a new magic number; if the synced span keeps growing, revisit the threshold or adopt an explicit empty-tail convention for lean repos. All commits local/unpushed. Process note for concurrent applies: give each session its own `git worktree`.
