# SMALL premise pass — prune-knowledge

```
OVERRIDE: proceed
```

**WAIVED by operator directive (2026-07-03, per-run).** The orchestrator's flash premise pass was
skipped at operator instruction; the operator authorized proceeding to apply without it (recorded
above as `OVERRIDE: proceed` so the apply skill's SMALL premise gate resolves). Not a standing
waiver — the default SMALL premise trigger stands for future changes unless the operator re-directs.

Context: the portfolio direction is already premise-gated (`plans/succession-hardening/explore-brief.md`,
pro `PREMISE: AGREE` 2026-07-02); prune-knowledge is the pre-approved AGGRESSIVE-pruning closer within
that vetted direction.
