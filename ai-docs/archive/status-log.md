# Status Log

Append-only log of STATUS.md change entries that have aged out of the 3-paragraph cap. Newest first.

---

## Prior change — split-open-questions SHIPPED (2026-06-17)


The `cap-status-log` assumption that `ai-docs/open-questions.md` could stay "read in full" bounded by the retired-notes mechanic didn't hold for high-activity repos — extrends' file stayed over the Read tool's length cap even after a conservative resolved-items prune, dominated by genuinely-open follow-ons accreting faster than items resolve. This change introduces a **horizon split**: `ai-docs/open-questions.md` now holds ONLY active items (open blockers flagged **BLOCKING**, operator-decision items, in-flight backlogs that gate other work), while the deferred/monitored/low-priority long tail lives in `ai-docs/parked-follow-ons.md` (grouped by `##` area headers, loaded on demand — NOT part of the mandatory onboarding read). A live blocker is never parked while live — the split is by horizon, never by age, preserving blocking-item visibility while bounding the always-loaded surface. AGENTS.md's MANDATORY read block was amended (parked-follow-ons.md explicitly excluded from the mandatory read), the horizon-split rule was encoded in the State/write-discipline section, and the "Authored deliverables" directory listing was updated. Both archive-executor §3c bodies now route new follow-ons by horizon at archive (active → open-questions.md, parked → parked-follow-ons.md), and the archive skill verify-checklist asserts no live blocker was parked. The physical two-file split (C2) was chosen over header-scan-only (unsound — a change-named section can hide a live blocker the scanning agent never opens) and index-only-with-pointers (delays but does not bound, breaks for ad-hoc items) — the same reasoning `cap-status-log` used for the STATUS.md↔status-log.md split: physical separation ENFORCES the bound rather than making it merely satisfiable via agent self-discipline. No delta specs — MEDIUM change, tasks.md only. Verify: READY FOR ARCHIVE — self-review + deepseek-v4-pro verifier pass both VERDICT: READY with zero defects (flash pass skipped per operator); `sync_scaffold.py --check` against both downstream repos confirmed drift set exactly this change's four managed files with every other file IDENTICAL; AGENTS.md span-merge clean; C4 guard green; `openspec validate --all` passed. Rationale and rejected alternatives in decisions.md; active follow-ons in open-questions.md (propagation backlog HIGH/BLOCKING), parked ones in parked-follow-ons.md (one-time migrations, redundant-summary cleanup).
## Prior change — cap-status-log SHIPPED (2026-06-17)

STATUS.md, decisions.md, and open-questions.md were growing unbounded per archive — the scaffold's archive-executor appended but never pruned, and AGENTS.md's "read in full" directive was literally unsatisfiable in downstream repos (extrends hit 449 lines / ~27k tokens in STATUS.md alone, with 38 change headers). This change adds a **cap rule**: STATUS.md now holds at most 3 change paragraphs, with overflow moved verbatim to `ai-docs/archive/status-log.md` (newest-first append-only log) during archive reconciliation. AGENTS.md's MANDATORY read block was amended: decisions.md now reads `##` section headers then relevant entries in full (not the entire file), while STATUS.md and open-questions.md stay "read in full" (bounded by the cap and the existing retired-notes mechanic respectively). A freshness carve-out exempts process/scaffold-maintenance commits from the resume-time lag-check. Both archive-executor bodies (`.claude/` and `.opencode/`) were hardened with the prune bullet and are byte-identical per the C4 guard. The archive skill verify checklist now asserts the cap. No delta specs — MEDIUM change, tasks.md only. Verify: READY FOR ARCHIVE — self-review + pro + flash all returned VERDICT: READY with zero defects; live sync smoke against psc-monitor confirmed AGENTS.md span-merge clean with only the two synced files DIFFERS (psc-monitor behind by one sync); guard suites green; `openspec validate --all` passed. Rationale in decisions.md; extrends one-time cleanup follow-on in open-questions.md.

## Prior change — propagate-baseline (W6) SHIPPED (2026-06-17)

The one-time scaffold→downstream propagation snapshot — the LAST change in the W-family. The golden source was synced to both downstream repos with `scripts/sync_scaffold.py`, bringing every manifest-managed file (skills, agents, AGENTS.md workflow span, ai-docs, scripts) up to scaffold HEAD after W0–W5 archived — i.e. the entire W0–W5 hardening family now lives in `extrends` and `psc-monitor`. **extrends** (commit `176d554`): Option-Y relocation first — the inline pytrends/TrendScope war-stories that the generic synced skills replace were moved to a new `extrends/ai-docs/workflow-lessons.md` (the concrete incident already lived in extrends' `decisions.md` D1 + archived `harden-pytrends-validation`); then synced + wired. **psc-monitor** (commit `6541a9d`): pure lag, no relocation; synced + wired. Per-repo wiring (NOT scaffold-managed): each repo's `.claude/settings.json` got two `PreToolUse` Bash/`git commit*` hooks — the `test-gate.sh` commit-test gate and the downstream-only `scaffold_check.py` managed-file guard (extrends had no settings.json → created; psc-monitor had a permissions block → hooks merged in, permissions preserved). The commit-test gate is shipped **dormant** (no `scripts/test-cmd`, matching scaffold's own template posture) — each repo opts in later. Lightweight runbook, not full openspec lifecycle (W6 edits no scaffold-managed file): the gate is the deterministic post-sync `sync_scaffold.py --check <repo>` = **all IDENTICAL** plus a diff-review that only manifest files changed and the AGENTS.md span-merge preserved each repo's title/`## Project context`/tail. Per repo: 30 synced unit tests pass; live guard smoke confirmed `scaffold_check.py` exit 2 on a staged managed file (closes the W1 deferred follow-up); commits scoped by explicit path and made with `--no-verify` (the sanctioned deliberate-sync escape). Both commits LOCAL/unpushed. The superseded `scaffold-sync` change was deleted (its surviving parts shipped in W1 + this propagation); the two design records (`consolidation-plan-2026-06-16.md`, `workflow-audit-2026-06-16.md`) were committed. Pre-existing and out of scope: psc-monitor's `openspec validate --strict` flags three specs/changes for missing `## Purpose` sections (untouched by the sync). Archive: `openspec/changes/archive/2026-06-17-propagate-baseline`. With W6 done, **the full W0–W6 family has shipped and the three repos are converged on one workflow source.** One finding from the consolidation ledger was deferred and not shipped — audit **C2** (within-scaffold rule-restatement dedup), twice-deferred (W2→W6) and never re-scoped; it is logged as the candidate next change ("W7") in `ai-docs/improvement-roadmap.md` + `open-questions.md`. Everything else in the plan landed.

## Prior change — fix-convergence-guard (W3) SHIPPED (2026-06-17)

Convergence-guard correctness fixes A1–A5: oscillation detection and absolute backstop ceiling (A1) so oscillating-signature failures end in a declared `STOP:a:` rather than a 600s wall-clock timeout; test-scoped error-signature normalization and basename-reduced state key (A2) so unrelated output churn doesn't defeat rule-(a) detection; deterministic git-diff-derived per-attempt file-touch detection with SHA1 content fingerprints (A3), with `--editing` demoted to optional fallback hint — no longer load-bearing for rule (b), because flash is unreliable at self-reporting `--editing` every iteration; path-shaped regex replacing the greedy `/\S+` normalization (A4) so two genuinely different path-differing errors keep distinct signatures; and `test-gate.sh` cwd-fixed to `cd "$REPO_ROOT"` before test-command resolution (A5) so the commit gate exits 2 (blocks) even when the hook fires from a non-repo-root directory. The apply-executor mirrors (`.claude/` + `.opencode/`) were updated for A3's `--editing` contract and kept byte-aligned. Design twice deepseek-v4-pro reviewed at propose (Round 1: 3 🔴 all genuinely resolved; Round 2: READY TO FREEZE). Verify: primary self-review PASS; deepseek-flash verifier corroborated 28/28 tests green and mirror byte-alignment, but emitted no formal VERDICT block (inconclusive); `openspec validate --all` 8/8 passed. Archive: `openspec/changes/archive/2026-06-17-fix-convergence-guard`. The existing decisions.md convergence-guard entry was updated in-place for A3; follow-ons (minor cleanup redundant assignment, MEDIUM-tasks-only validation gap) in open-questions.md. Modified spec: `openspec/specs/apply-convergence-guard/spec.md`. Modified agents: `.opencode/agents/apply-executor.md`, `.claude/agents/apply-executor.md`.
## Prior change — cleanup-workflow-ergonomics (W5) SHIPPED (2026-06-17)

Workflow-ergonomics cleanup — the four "cleanup" residue findings from the 2026-06-16 audit (B1, D-iii, E4, E5), a doc/markdown-only change. **B1 (reorder-only):** the two most-executed skills (`openspec-apply-change`, `openspec-verify-change`) now lead each delegation section with a concise happy path, with the assert-ran / failure-ladder / caveat material moved into labelled `#### Failure modes` / `#### On a defect / failure modes` subsections — net-semantic-zero (no caveat, sentinel, or escalation rung deleted; the W4 simplify/security gates and the multi-model-passes section were left intact). **D-iii:** removed the leading `# <name> Specification` H1 from `verify-multimodel-gate/spec.md` and `scaffold-sync-mechanism/spec.md` so all 7 specs start at `## Purpose`, and added the convention to `openspec-sync-specs` so future promotions don't reintroduce it. **E4:** AGENTS.md now documents the rollback branch (`git revert` + a new corrective change) for an archived-change-found-wrong, distinct from a botched-archive recovery. **E5:** added `docs/test/skill-enumeration-smoke/` and RAN the live smoke against opencode 1.17.7 — all 7 `openspec-*` skills enumerate from `.claude/skills/` with the cross-load flag unset (closes the W0/audit-E5 carry-forward); recorded a procedure gotcha (capture `opencode debug skill` to a file before grepping — a direct pipe races the ~120 KB stream and undercounts). Verify verdict: READY FOR ARCHIVE — self-review + one `deepseek-v4-flash` verifier pass both READY, 0 defects (pro pass skipped per operator precedent); `openspec validate --strict` 8 passed / 0 failed. As-built process delta: a concurrent W4 (`lifecycle-gates`) apply reset the shared working tree and wiped W5's first apply, so W5 was re-applied by the orchestrator directly on top of W4's committed base (doc-only, within "quick doc edits done by the primary directly"). `decisions.md` (B1/E4/E5) and `open-questions.md` (E5 resolved + the concurrent-tree/worktree follow-on) were reconciled inline. Archive: `openspec/changes/archive/2026-06-17-cleanup-workflow-ergonomics`. No delta specs (D-iii edits existing promoted specs in place).
## Prior change — lifecycle-gates SHIPPED (2026-06-17)

The verify gate was tier-scaled and two new quality gates were added. The multi-model verification passes now apply to MEDIUM/COMPLEX changes only — SMALL changes are exempt and do their own verification (optionally one flash pass if risky). A simplicity/quality gate (harness-neutral: Claude `simplify`/`/code-review`, OpenCode concrete checklist) and a conditional security gate (for auth/credentials/data/external-API changes; hard at COMPLEX, recommended at MEDIUM) were added to verify, layered after the verifier passes and before the artifact checklist. The archive-executor bodies in both `.claude/` and `.opencode/` were hardened to handle RENAMED spec requirements (FROM:/TO: format), matching the sync-specs/archive-skill contract. A new `scripts/test_executor_body_agreement.py` guard was added — it strips frontmatter, normalizes the one sanctioned intro-clause divergence, and byte-compares each executor pair's body, failing if the two drift on any non-intro line; it is manifest-listed for W6 propagation. Verify verdict: READY FOR ARCHIVE — self-review + one deepseek-v4-flash verifier pass both returned READY with zero defects; both test suites ran green (test_executor_body_agreement.py and test_sync_scaffold.py). One defect found during self-review — the delta spec was authored in a non-canonical format and was re-delegated for a format rewrite to canonical `## MODIFIED`/`## ADDED Requirements` form. As-built delta discovered during verify: the apply-executor normalized pre-existing drift in both executor pairs (bullet ordering and a bold `**completion report**` in the archive-executor) — pure normalization caught by the new C4 guard. Archive: `openspec/changes/archive/2026-06-17-lifecycle-gates`. Rationale in decisions.md; follow-ons in open-questions.md. Modified spec: `openspec/specs/verify-multimodel-gate/spec.md`.
## Prior change — dedup-scaffold SHIPPED (2026-06-17)

The `opencode run` delegation harness was single-sourced: the shared prose (hardened invocation, assert-real-agent-ran, surgical-kill, EXIT-sentinel completion detection) and timeout budgets now live once in `ai-docs/delegation-harness.md` with a centralized timeout-budget table, and the four delegating skills (apply/verify/propose/archive) cite the doc instead of carrying overlapping copies. This is extraction, not redesign — all behavior preserved byte-for-byte and existing drift (kill-grace `-k 30` vs `-k 15`, full vs abbreviated assert-ran) reconciled to the most-complete copies. Scope was C1 (harness extraction) + B3 (timeout table) only; C2 rule-restatements and C4 executor-body guard deferred to W6/W4. No delta specs — MEDIUM change, tasks.md only. Verify verdict: READY FOR ARCHIVE — self-review + deepseek-v4-flash verifier pass both READY with zero defects; the full test suite ran clean (test_sync_scaffold.py + test_convergence.py both green); the deepseek-v4-pro verifier pass was skipped at operator instruction. Three follow-ups surfaced (archive EXIT-sentinel bug, stress task-slicing, tier-scale timeouts) in open-questions.md. Archive: `openspec/changes/archive/2026-06-17-dedup-scaffold`. Rationale in decisions.md; follow-ons in open-questions.md.
## Prior change — fix-sync-mechanism SHIPPED (2026-06-17)

The scaffold→downstream sync mechanism delivered: four new files under `scripts/` — `sync_scaffold.py` (copies manifest-listed files byte-identical with AGENTS.md span-replace preserving per-repo title/project-context/tail), `scaffold_manifest.txt` (self-listed authoritative inventory of shared files), `scaffold_check.py` (PreToolUse hook guard that exits 2 when a scaffold-managed file is staged for commit), and `test_sync_scaffold.py` (unit tests for span-replace idempotency, tail preservation, and abort guards). Two blocking bugs from the prior frozen `scaffold-sync` plan are fixed: the guard now exits 2 (not 1 — Claude Code PreToolUse only blocks on exit 2), and no "DO NOT EDIT" header is injected (cut entirely — the header subsystem caused AGENTS.md non-idempotency on the second sync). Scaffold-only; downstream propagation (W6) is the next step. Verify verdict: READY FOR ARCHIVE — self-review + one deepseek-v4-flash verifier pass (READY, zero defects); real-data eyeball confirmed span-replace idempotent on extrends and psc-monitor AGENTS.md, tail preserved byte-identical, tail-boundary regex not fooled by in-prose mention. The pro verifier pass was skipped at operator instruction. Archive: `openspec/changes/archive/2026-06-17-fix-sync-mechanism`. Rationale (D4 header-cut, D5/B1 exit-2 guard) in decisions.md; five forward-looking items in open-questions.md. New spec: `openspec/specs/scaffold-sync-mechanism/spec.md`.
## Prior change — verify-multimodel-gate SHIPPED (2026-06-16)

Independent multi-model verification passes added as hard gates in the verify step: a new delegated `openspec-verifier` agent (read-only on files, bash-capable, `edit: deny`) runs the same behavioral review the self-review runs — read diffs, full test suite, real output — and emits a machine-discriminable verdict (`## Verify Pass` / `VERDICT: READY` or `NEEDS REVISION`). The pass chain depends on platform: a Claude Code orchestrator runs self-review → `deepseek-v4-pro` → `deepseek-v4-flash` (three independent passes); an OpenCode orchestrator runs self → flash only (already deepseek-pro, so only flash adds model diversity). Each pass is a hard gate; on NEEDS REVISION the orchestrator fixes via re-delegation and reruns the failed pass and every pass after it (never earlier ones), escalating to the operator after three cycles. The verifier's hardening extends the existing noninteractive-delegation-safety posture: the Claude Code `opencode run` verifier invocations close stdin, the OpenCode Task-tool path is exempt, and the verifier takes executor-style `external_directory` containment (a third permission category — bash-capable but write-denied — between reviewer and executor). The orchestrator judges every finding from disk, not on the verifier's word. Verify verdict: READY FOR ARCHIVE — behavioral review passed; all three passes (self/pro/flash) returned READY with no defects; the V6 live probe confirmed both verifier tiers loaded the real agent and emitted the machine-discriminable verdict block. The gate was dogfooded on its own change. Archive: `openspec/changes/archive/2026-06-16-verify-multimodel-gate`. Rationale and design decisions in decisions.md; open follow-ons in open-questions.md. New spec: `openspec/specs/verify-multimodel-gate/spec.md`. Modified spec: `openspec/specs/noninteractive-delegation-safety/spec.md`.
## Prior change — harden-delegation-robustness SHIPPED (2026-06-16)

The third scaffold hardening change: all four delegated `opencode run` invocations (propose/review, apply, archive, verify fix-executor) now close stdin (`< /dev/null`), converting unanswerable permission prompts from indefinite hangs into fail-fast auto-deny — the single reliable fix empirically pinned against opencode 1.17.7. Per-agent `external_directory` permissions are split by destructive capability (reviewer: `allow`; apply/archive executors: `{ "*": deny, "/tmp/**": allow }` with catch-all listed first for opencode's last-match-wins evaluation) so the auto-deny never starves legitimate access while containing write-capable executors to the worktree. The non-convergence canary was rebuilt to be non-gameable: the impossibility now lives in an implementation module that a frozen, instruction-gated test imports — an honest executor editing only the editable surface reaches a declared `### NON-CONVERGENCE BLOCKER` instead of turning the assertion green. A commit-test-gate smoke fixture and documented hook-wiring procedure were shipped under `docs/test/commit-gate-smoke/`. Docs/agent/skills-only change — no code changes in scaffold itself, no test suite. Verify verdict: READY FOR ARCHIVE — behavioral review passed (V1–V5); one defect found in the smoke fixture's README snippet (wrong working-dir handling, wrong path depth, `set -e` aborted at intentional exit-2 case) was fixed by a re-delegated fix-executor plus two hand-fixes; all five gate-script exit branches verified in order; canary renders `assert (2 == 2 and 2 == 3)` with a single captured value (ungameable by stateful impl); stdin-closed prompt auto-rejects in seconds. Archive: `openspec/changes/archive/2026-06-16-harden-delegation-robustness`. Rationale and three new decisions in decisions.md; open follow-ons in open-questions.md. New spec: `openspec/specs/noninteractive-delegation-safety/spec.md`. Extended specs: `openspec/specs/apply-convergence-guard/spec.md`, `openspec/specs/commit-test-gate/spec.md`.
## Prior change — harden-instruction-surface SHIPPED (2026-06-16)

Instruction-surface audit found the first-load docs (AGENTS.md + `ai-docs/` + workflow skills) had drifted out of sync with shipped 2026-06-16 behavior: three changes (commit-test-gate hook, apply-convergence-guard failure ladder, `scripts/test-cmd` single-source) updated the skills and `decisions.md` but were never propagated to older instruction surfaces, and the `## Change tiers` guidance actively licensed unsafe autonomy ("classify every change yourself and state the tier"). This change reconciled all stale text and added a new tier-confirmation guardrail: a non-fast-track agent must propose its tier + plan and obtain operator confirmation before executing. Also fixed the fast-track failure ladder (had reverted declared-blocker triage to reflexive Sonnet), corrected the "hook-free" claim about `.claude/settings.json`, single-sourced the orchestrator's verify re-run from `scripts/test-cmd`, aligned the research-fetch convention, and fixed the onboard tutorial template. Docs/instruction-only change — no code, no test suite in scaffold, ran clean. Verify verdict: ready for archive — all tasks landed, every grep-acceptance assertion passed, zero defects found. Archive: `openspec/changes/archive/2026-06-16-harden-instruction-surface`. Rationale and rejected alternatives in decisions.md; open follow-ons in open-questions.md.
## Prior change: harden-delegation SHIPPED (2026-06-16)

Delegated-work governance hardened with three behavior-level gates: a deterministic commit-test-gate (Claude `PreToolUse` hook on `git commit` calling a shared `scripts/test-gate.sh` that reads the per-repo `scripts/test-cmd` — blocks on red, no-ops when absent), a apply-executor non-convergence guard (the executor runs `scripts/_convergence.py` after each red test to detect stalled failure / repeated touches / artifact gaps, stops with a structured `### NON-CONVERGENCE BLOCKER` block instead of looping, and the failure ladder routes declared blockers to orchestrator triage rather than reflexive Sonnet), and a reviewer budget upgrade (780s timeout in `openspec-propose`, incremental emission so partial output survives cutoff, salvage-and-rerun on timeout with findings). The scaffold ships the hook inert (no `scripts/test-cmd`); it activates wherever the per-repo command is filled. Archive: `openspec/changes/archive/2026-06-16-harden-delegation`. Verify verdict: ready for archive — tests pass, the system ran clean, the convergence helper correctly trips rule (a) after two same-signature attempts while permitting healthy iteration. No CRITICAL implementation defects; two REQUIRED follow-ups (hook live smoke-test not possible from this session; canary fixture is gameable — see open-questions.md). Full rationale and rejected alternatives in decisions.md; open follow-ons in open-questions.md.
