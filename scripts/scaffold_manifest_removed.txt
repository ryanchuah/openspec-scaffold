# Removed scaffold-managed inventory — one repo-relative path per line.
# '#' and blank lines are ignored. A trailing '/' marks a directory entry;
# anything else is a file entry. Files/directories listed here must NOT
# exist in downstream repos: sync() will delete them and --check will
# report them as STALE.

# Seed: openspec-onboard skill was removed from scaffold in the
# prune-knowledge portfolio (2026-07-03). Downstream copies still exist,
# notably in psc-monitor.
.claude/skills/openspec-onboard/

# checks-facts-split (2026-07-03): audit_bundle.py and its test file
# renamed to checks.py / test_checks.py; facts.py is the facts surface.
scripts/audit_bundle.py
scripts/test_audit_bundle.py

# clarify-audit-tooling-surface (2026-07-03): the lint-knowledge skill was
# renamed to knowledge-drift-review. Downstream copies still carry the old
# skill dir (extrends, psc-monitor) — delete it on sync.
.claude/skills/lint-knowledge/

# split-outstanding-work-skills (2026-07-16): outstanding-work-review was
# renamed/narrowed to outstanding-work-scan, with a new sibling
# outstanding-work-deep-sweep skill for the heavy residual sweep. Downstream
# copies still carry the old skill dir — delete it on sync.
.claude/skills/outstanding-work-review/
