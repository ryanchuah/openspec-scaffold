## 1. Reviewer mandate — `.opencode/agents/openspec-reviewer.md`

- [x] 1.1 In `## What to Check`, after the per-artifact subsections (`For proposal.md` / `For design.md` / `For tasks.md`), add a `### Premise review (direction artifacts: explore-brief.md, proposal.md, SMALL plans)` subsection — this wider title supersedes design D3's narrower `(proposal.md and SMALL plans only)` parenthetical, per D1/D7 which add `explore-brief.md` as a direction artifact — with the four checks (D3): (1) root, not symptom; (2) solution targets the root; (3) scope right-sized + out-of-scope stated; (4) name the blind spot. State the framing as **default-to-dissent** (say where the framing is wrong and why; do not manufacture agreement) and the **calibration** (D4): dissent only for genuine direction faults, never style/taste/wording. Note that a SMALL plan provides the premise material as problem / approach / out-of-scope; a plan missing those is a structural finding (not a silent guess).
- [x] 1.2 Within that same subsection, add (a) the **drift** instruction (D10): when handed a verified `explore-brief.md` as context, also flag drift = the artifact reframes the problem, switches to a ruled-out approach, or expands scope beyond what was vetted — narrower scope / restatement / added detail are NOT drift; drift surfaces as `PREMISE: DISSENT`; and (b) the **explore-altitude calibration** (D11): dissent only when the direction is demonstrably wrong given available info, NOT merely under-specified (expected at explore); where scope is not yet concrete, note "scope not yet concrete — deferred to proposal".
- [x] 1.3 In `## Output Format`, after the `### Verdict` block, add a `### Premise Verdict` section emitting exactly one line `PREMISE: AGREE` or `PREMISE: DISSENT` followed by cited concerns (or `- None`). State it is emitted when reviewing a **direction artifact** (`explore-brief.md` / `proposal.md` / SMALL plan), orthogonal to the 🔴/🟡/💡 severity verdict.
- [x] 1.4 In `## Anti-Patterns to Avoid`, add the read-only-honesty caveat (D3): the reviewer is `bash: deny` and reasons about root cause from reading code — it MUST NOT claim to have reproduced/confirmed/empirically verified a root-cause or behavioral assertion (extends the existing "affirming unverifiable empirics" anti-pattern).

## 2. Explore direction gate (Altitude 1) — `.claude/skills/openspec-explore/SKILL.md`

- [x] 2.1 Augment the existing `PHASE GATE` block (D7): when exploration crystallizes, **offer** the operator an explicit advancement choice (the operator may also invoke it directly) — not keyword-sniffing. On that choice, write `plans/<slug>/explore-brief.md` (`<slug>` = kebab-case derived from the exploration topic, the propose-skill convention; `mkdir -p` on first write; disambiguate on collision). Preserve "no mandatory output" for idle exploration (no choice → no brief, no review). Do NOT surface the propose/apply hint until the verdict is resolved.
- [x] 2.2 Invoke the reviewer (D7): `opencode run --agent openspec-reviewer --model deepseek/deepseek-v4-pro` under the harness (`< /dev/null`, `--dir <repoRoot>`, capture stdout/stderr to separate files, assert-ran), `timeout -k 15 780`. This is a **synchronous** call (like the propose reviewer) — no background EXIT-sentinel. Prompt names `plans/<slug>/explore-brief.md` and signals "premise review — direction". Extract the `### Premise Verdict` block from stdout to `plans/<slug>/premise-review.md` (reviewer is `edit: deny`); on timeout/crash apply the propose-reviewer salvage rule, writing the partial marked `PARTIAL`.
- [x] 2.3 DISSENT handling + advancement hint (D9): on `PREMISE: DISSENT`, present cited concerns via **AskUserQuestion** with options re-think / re-scope / override-to-proceed. On override, append a `### Resolution` section to `premise-review.md` with a single line `OVERRIDE: proceed — <rationale>`; re-think/re-scope loop back to revise the brief and re-review (no `OVERRIDE` line). Once resolved (`AGREE` or `OVERRIDE: proceed`), surface the advancement hint **naming the slug** (e.g. "Direction captured as `add-user-auth` — say 'propose add-user-auth' when ready").

## 3. Propose skill — `.claude/skills/openspec-propose/SKILL.md`

- [x] 3.1 In the reviewer invocation step (3c), update the prompt so that for `proposal.md` it requests the premise assessment (the `### Premise Verdict` block); and when a verified `explore-brief.md` exists for the change, pass it as context with the D10 "also flag drift" instruction.
- [x] 3.2 Extend the "assert the real reviewer ran" check (3c) so that, for `proposal.md`, it also asserts a `PREMISE: AGREE|DISSENT` line is present; a missing premise line is a failed pass — do not freeze, re-run (D2a).
- [x] 3.3 Change the freeze condition **for `proposal.md` only** to: zero 🔴 **and** `PREMISE: AGREE`. Leave `design.md` / `tasks.md` on the existing zero-🔴 rule (D2).
- [x] 3.4 Add the `PREMISE: DISSENT` branch (D2b): stop the freeze loop, present concerns via **AskUserQuestion** (re-frame / re-scope / override-to-proceed), record in `review-log.md`; only override permits freezing on a dissent, after which the freeze condition reverts to zero 🔴 only (dissent not re-litigated).
- [x] 3.5 In the change-creation step (after `openspec new change`), relocate `plans/<slug>/explore-brief.md` and `premise-review.md` into the change dir (D8), using the slug from the operator's advancement hint; best-effort **skip** when no matching `plans/<slug>/` exists.

## 4. SMALL premise pass (Altitude 2) — harness, AGENTS.md, apply gate

- [x] 4.1 In `.claude/skills/_shared/delegation-harness.md`, add **two** budget-table rows — `explore | direction gate (pro) | -k 15 780` and `SMALL | premise reviewer (flash) | -k 15 780` — each at 780s / 15s, with a one-line note that these are keyed by stage/tier because they run outside the named phases (per D6).
- [x] 4.2 In `AGENTS.md` **SMALL** tier bullet — invocation: the orchestrator runs the flash premise pass after the plan is written and before apply delegation (trigger independent of operator confirmation); inline the exact `opencode run --agent openspec-reviewer --model deepseek/deepseek-v4-flash` command (hardened pattern, citing the harness + the 4.1 row) with the prompt *"Review the plan at `<planPath>`. This is a SMALL change plan — NOT a structured proposal.md. Emit a `### Premise Verdict` block (PREMISE: AGREE|DISSENT) assessing problem/root-cause/solution."*; extract and write only the verdict block to `premise-review.md`. Add a `Plan minimum:` sub-point (problem / approach / out-of-scope). If a verified `explore-brief.md` exists, pass it with the D10 drift instruction.
- [x] 4.3 In the same `AGENTS.md` **SMALL** bullet — verdict routing + resilience: without an autonomy grant the verdict is presented inside the operator tier+plan confirmation; under an autonomy grant the grant covers tier self-classification only — a `DISSENT` with no recorded `OVERRIDE: proceed` leaves the apply gate stopped and the orchestrator escalates. State the timeout/crash salvage: partial → `premise-review.md` marked `PARTIAL` (re-run once if >120s or any finding; else escalate).
- [x] 4.4 In `.claude/skills/openspec-apply-change/SKILL.md`, add the SMALL **gate** as a new step **immediately after Step 1 (change selection)** — fail-fast, before the context reads of Steps 2–5: for a SMALL change, assert `premise-review.md` exists with a resolved verdict (`AGREE` or a recorded `OVERRIDE: proceed`) and STOP otherwise. State explicitly that the apply skill does **not** invoke the premise reviewer (the orchestrator already did); MEDIUM/COMPLEX skip this gate.

## 5. Cross-cutting AGENTS.md — lifecycle, roles, acknowledgements

- [x] 5.1 In the OpenSpec workflow / Roles sections, name the **two-altitude** pre-implementation premise gate: the **direction gate** at explore (pro review of a load-bearing `explore-brief.md`, all tiers) and the **change-itself** checks (folded into the pro proposal review for MEDIUM/COMPLEX; the flash pre-apply pass for SMALL) — the direction-level counterpart to the post-implementation `verify-multimodel-gate`.
- [x] 5.2 In the "After reading this file" acknowledgements, add that direction is premise-reviewed at explore (before any change is written) and the change itself at propose/apply, with `DISSENT` surfaced to the operator.

## 6. Validation sanity (executor self-check)

- [x] 6.1 Run `openspec validate premise-review-gate` and confirm it reports valid.
- [x] 6.2 Run `python3 scripts/scaffold_check.py` and confirm it passes for the edited scaffold-managed files (golden-source edits must not trip the guard); report any drift rather than suppressing it.
