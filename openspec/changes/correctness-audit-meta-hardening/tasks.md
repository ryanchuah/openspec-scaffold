## 1. knowledge_lint — audit-liveness drift check (Delta 1 detector)

- [x] 1.1 Add a module-level helper `_active_questions_text(root)` to `scripts/knowledge_lint.py` that returns the text of the `## Active` section of `knowledge/questions/INDEX.md` (the region from the `## Active` heading up to the next `## ` heading), or an empty string when the file or the section is absent. Read-only; no write-call tokens. Wrap the `read_text(...)` in `try/except OSError: return ""`, mirroring the existing `_check_audit_dossier` idiom.
- [x] 1.2 Add `_check_audit_liveness(root: Path) -> list[Finding]` mirroring the guarded `_check_audit_dossier` idiom: glob `knowledge/research/correctness-audit-*/`; consider only dirs whose `CHARTER.md` contains the literal `format: correctness-audit/v1`; skip any whose `CHARTER.md` also contains a `status: closed` line. For each remaining (in-progress) dossier, require the dossier directory basename (e.g. `correctness-audit-2026-07`) to appear (substring membership) in `_active_questions_text(root)`; if it does not, append `Finding("audit-liveness", <dossier relpath>, None, "in-progress correctness-audit dossier not referenced by an Active knowledge/questions/INDEX.md item")`. Empty glob / markerless / charter-less / closed → no findings. Detect-only. Add a one-line comment noting the match is substring membership on the unique `correctness-audit-YYYY-MM` basename.
- [x] 1.3 Wire `_check_audit_liveness(root)` into `collect_findings()` by appending `findings.extend(_check_audit_liveness(root))` after the existing `_check_untriaged_age` line.

## 2. knowledge_lint — post-close ledger format check (Delta 4 detector)

- [x] 2.1 Add `_check_post_close_ledger(root: Path) -> list[Finding]` to `scripts/knowledge_lint.py`: glob `knowledge/research/correctness-audit-*/`; for each dir whose `CHARTER.md` contains `format: correctness-audit/v1`, look for `POST-CLOSE-LEDGER.md`. When present, treat as an entry line every line that is NOT blank, NOT a markdown heading (stripped line starts with `#`), NOT a table-separator row (stripped line consists only of `|`, `-`, `:`, spaces), and NOT an HTML comment (stripped line starts with `<!--`). For each entry line: strip it, remove a single leading `|` and a single trailing `|` if present, split on `|`, and require at least five cells each non-empty after trimming — so both `a | b | c | d | e` and `| a | b | c | d | e |` are accepted. Otherwise append `Finding("post-close-ledger-format", <ledger relpath>, lineno, "malformed post-close ledger line (need commit | subsystem | wave-owner | spec? | review-tier): <line>")`. Absent ledger / unmarked / no dossier → no findings. Detect-only. (Rationale for "at least five" rather than "exactly five": it is the false-positive-safe posture under the live-tree gate — the per-cell non-empty check already rejects stray-pipe empty cells, and a benign extra field never reddens the gate. Do not tighten to "exactly five" without re-checking that trade-off.)
- [x] 2.2 Wire `_check_post_close_ledger(root)` into `collect_findings()` by appending `findings.extend(_check_post_close_ledger(root))` after the `_check_audit_liveness` line.

## 3. Tests for the two new checks (scripts/test_knowledge_lint.py)

- [x] 3.1 Add liveness tests mirroring the dossier three-tier + positive pattern, each building a marked dossier via the existing `_build_dossier` helper and writing `knowledge/questions/INDEX.md` via `_write_tree`: (a) no dossier dir → no `audit-liveness` findings; (b) markerless charter → clean; (c) charter containing `status: closed` → clean even with no Active item; (d) in-progress dossier (marked, no `status: closed`) whose dir name is absent from the INDEX Active section → exactly one `audit-liveness` finding; (e) in-progress dossier whose dir name appears in the `## Active` section → clean; (f) in-progress dossier whose dir name appears only under `## Parked` (not `## Active`) → flagged.
- [x] 3.2 Add post-close ledger tests: (a) marked dossier with no `POST-CLOSE-LEDGER.md` → no `post-close-ledger-format` findings; (b) ledger whose entry lines each carry five non-empty cells, in BOTH the bare form (`a | b | c | d | e`) and the pipe-delimited table form (`| a | b | c | d | e |`) → clean; (c) an entry line with fewer than five cells, and one with an empty cell → each flagged with its line number; (d) heading / table-separator (`|---|---|`) / HTML-comment / blank lines present alongside valid entries → not treated as entries, clean.
- [x] 3.3 Confirm the existing `test_module_source_has_no_write_calls` static guard still passes — the two new functions and the helper introduce none of the forbidden write-call substrings anywhere in `knowledge_lint.py` (including comments/docstrings).
- [x] 3.4 Run the full test suite green via `bash scripts/check.sh` (ruff + pytest), including `scripts/test_doc_lint_gate.py`'s live-tree gate — both new checks MUST lint clean on this repo's real tree (this repo has no `correctness-audit-*` dossier, so both are guarded to zero findings). Stop on first failure.

## 4. correctness-audit SKILL.md prose edits — ORCHESTRATOR-APPLIED (NOT for the delegated executor)

> These six edits touch the load-bearing, downstream-propagated `.claude/skills/correctness-audit/SKILL.md`. They are applied **directly by the orchestrator** (load-bearing scaffold protocol prose with nested code fences), NOT by the flash apply-executor. The delegated executor implements sections 1–3 only. Exact verbatim insertion blocks live in this change's `skill-additions.md`.

- [x] 4.1 EDIT 1 + EDIT 2 — charter template: add the `status: in-progress` marker line and the discovery/remediation wave-namespace note.
- [x] 4.2 EDIT 3 — Wave 0: seed the liveness Active `knowledge/questions/INDEX.md` item and set the charter `status:`.
- [x] 4.3 EDIT 4 — Close-out: the blind coverage-gap review step (Delta 2) with the four dimension markers and the both-halves method note.
- [x] 4.4 EDIT 5 + EDIT 6 — Close-out: seed `POST-CLOSE-LEDGER.md` (Delta 4) and flip charter `status:` to `closed` + retire the Active item (Delta 1).
- [x] 4.5 EDIT 7 — Scope-seeding checklist appendix (Delta 3): the 11-group dimension seed and the named blind-spot classes, with classes 9–12 carried strictly as awareness pointers to the sibling `product-audit` skill (no claims-ledger mechanism built here). After inserting, verify the new `## Scope-seeding checklist` section terminates cleanly (single blank line) before the existing `## Guardrails` section and introduces no unbalanced code fences.
- [x] 4.6 EDIT 8 + EDIT 9 — Guardrails: sanction the new writes; add the `POST-CLOSE-LEDGER.md` template subsection (with ordinary triple-backtick fences).
