## 1. `apply_delta_spec.py` — the deterministic promoter

- [ ] 1.1 Create `scripts/apply_delta_spec.py` (stdlib-only, `argparse`, `sys.exit(main())`): flags `--change-dir` (required), `--specs-root` (default `openspec/specs`), `--dry-run`, `--json`. Exit codes: `0` clean plan (applied or nothing-to-do), `2` any anomaly. (A malformed invocation yields argparse's own exit 2 with no report — callers disambiguate an anomaly from a usage error by the presence of a valid report, not the code alone.) `--help` summarizes the D4 operation semantics (apply / skip / anomaly / defer).
- [ ] 1.2 Implement delta parsing: define own copies of the three `### Requirement:`-grammar regexes, copying the **exact** pattern strings from `checks.py` lines 1173–1175 verbatim — `_SECTION_HEADER_RE = r"^## (ADDED|MODIFIED|REMOVED|RENAMED) Requirements$"`, `_REQUIREMENT_HEADER_RE = r"^### Requirement:\s*(.*)"`, `_SCENARIO_HEADER_RE = r"^#### Scenario:"` — and a **separate** RENAMED list-item parser for `- FROM: \`### Requirement: X\`` / `- TO: \`### Requirement: Y\`` (extract the backtick-wrapped header names). Discover `<change-dir>/specs/<cap>/spec.md` files; parse each into per-capability ADDED / REMOVED / RENAMED / MODIFIED operation lists (D3).
- [ ] 1.3 Implement main-spec parsing: split `<specs-root>/<cap>/spec.md` into preamble (`## Purpose` etc.) and requirement blocks keyed by name; block boundary = next `### Requirement:` / next `## ` header / EOF; tolerate a main spec with no `## Requirements` header (zero requirements) without crashing (D9).
- [ ] 1.4 Implement the planner (D4 truth table) building post-promotion content in memory per spec: ADDED → apply / skip (body norm-equal) / anomaly (body differs); REMOVED → apply / skip (absent); RENAMED → apply / skip (already-renamed) / anomaly (neither or both); MODIFIED → defer. Body comparison normalizes per-line trailing whitespace + leading/trailing blank lines. Handle: new-main-spec creation (`## Purpose` TBD + `## Requirements` skeleton) for ADDED on an unknown capability; **REMOVED/RENAMED targeting a nonexistent main-spec file → anomaly** (nothing to modify) (D9); and intra-delta self-collisions via the same incremental in-memory plan (D4 note).
- [ ] 1.5 Implement the all-or-nothing writer + report (D6, D11): if any anomaly across any spec → write nothing, emit report, exit 2; else write each touched main spec atomically (temp file in same dir + `os.replace`) and exit 0. Emit the D11 JSON report under `--json`; otherwise a human summary that lists **anomalies first**, then applied / skipped / deferred. `--dry-run` runs the full plan + report + exit code but writes nothing.

## 2. `archive_move.py` — the deterministic move

- [ ] 2.1 Create `scripts/archive_move.py` (stdlib-only, `argparse`, `sys.exit(main())`): flags `--change-root` (required), `--archive-path` (required). Assert the source exists (else exit `2`), assert the destination does **not** exist (conflict → exit `2`, move nothing) — **both guard failures use exit code 2** — `mkdir -p` the destination parent, then `shutil.move` the directory; exit `0` on success. Print a one-line result (moved-to path, or the guard failure reason).

## 3. Tests + manifest registration

- [ ] 3.1 Create `scripts/test_apply_delta_spec.py` — the **core contract suite** (executor scope; the orchestrator adds independent adversarial/boundary fixtures at verify per the note below): each D4 row (ADDED apply/skip/anomaly, REMOVED apply/skip, RENAMED apply/skip/anomaly×2), MODIFIED deferred-not-applied, `--dry-run` writes nothing, JSON report shape + status/exit-code agreement (exit 2 on anomaly), new-main-spec creation, and the regex-agreement test (`import checks`; assert the three shared pattern strings equal `checks._SECTION_HEADER_RE.pattern` etc.). Use the repo's test convention (import + call `main()`; `tmp_path`-style fixtures). NOTE: the deeper adversarial fixtures — atomicity under a mixed clean+anomaly delta set, intra-delta self-collision, RENAMED+MODIFIED combo ordering, empty/degenerate deltas, only-`## Purpose` main spec, and the RENAMED-debut edges — are authored by the **orchestrator at verify** (an independent second source per `config.yaml rules.verify` + proposal.md), and are **not** part of this apply task.
- [ ] 3.2 Create `scripts/test_archive_move.py`: move success (dest parent created, dir relocated, exit 0), destination-exists conflict (exit 2, nothing moved), missing source (exit 2).
- [ ] 3.3 Register the four new files in `scripts/scaffold_manifest.txt` (in the scripts block): `scripts/apply_delta_spec.py`, `scripts/archive_move.py`, `scripts/test_apply_delta_spec.py`, `scripts/test_archive_move.py`.

## 4. Rewire the archive / sync-specs surface (fence-heavy prose)

<!-- Apply-split (HANDOFF lesson): the ORCHESTRATOR authors group 4 directly and checks each box
     BEFORE delegating apply, because flash struggles with fence-heavy skill prose. The apply-
     executor therefore implements groups 1–3 only and finds group 4 already `[x]`. These are
     still apply-phase tasks — they are pre-completed, not skipped. -->


- [ ] 4.1 `.claude/skills/openspec-sync-specs/SKILL.md`: keep the `openspec` CLI change-discovery (Steps 1–3); rewrite Step 4 so ADDED/REMOVED/RENAMED promotion calls `apply_delta_spec.py` (subprocess) and the LLM path handles only MODIFIED merges; replace the Step-4c "if requirement already exists → update it to match (treat as implicit MODIFIED)" prose with the D5 BREAKING contract (norm-equal → skip; differing → anomaly; never silent overwrite).
- [ ] 4.2 `.claude/skills/openspec-archive-change/SKILL.md`: rewire Step 4 to run `apply_delta_spec.py --dry-run --json` for the pre-prompt sync assessment (surface applied/skipped/deferred/anomalies to the operator); rewire Step 5's delegated flow to the D7 order — promote via `apply_delta_spec.py` (from the active change dir) → LLM MODIFIED merges → move via `archive_move.py` → reconcile docs from the moved archive path. Preserve the Step 5.0 checkpoint and the recovery block.
- [ ] 4.3 Rewrite BOTH `.claude/agents/archive-executor.md` and `.opencode/agents/archive-executor.md` "Your job" steps into the **D7 promote-then-move order** — renumbering the current move-then-sync steps: new step 1 = promote ADD/REM/REN via `apply_delta_spec.py` (from the active change dir) and merge only the report's `deferred_modified` requirements by hand (LLM); new step 2 = move the change dir via `archive_move.py`; then the existing reconcile-docs step (now reading from the moved archive path). Do **not** leave "step 1 = move" anywhere. Keep the two bodies **byte-identical** (except the sanctioned frontmatter + intro-clause divergence); after editing, run `python3 scripts/test_executor_body_agreement.py` and confirm it passes.
