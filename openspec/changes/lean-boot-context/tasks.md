## 1. P3 — Add state-file bounding rules to their canonical home (scaffold)

- [x] 1.1 In `/home/pang/Projects/openspec-scaffold/AGENTS.md`, section `## State, write discipline, and the archive-as-handoff rule`, append to the very end of the paragraph that begins with the bold label `**STATUS.md cap rule:**` (that paragraph ends with "…and `decisions.md` is intentionally append-only.") a new sentence — "Each retained change-entry section is a ≤150-word headline summary (what shipped, key verify outcome, where details live); prose beyond that budget moves to `ai-docs/archive/status-log.md` at archive time."
- [x] 1.2 In the same section, append to the end of the **open-questions.md horizon-split rule** paragraph: "At archive, every non-BLOCKING bullet in a shipped-change section parks to `ai-docs/parked-follow-ons.md`, leaving a one-line pointer stub of the form `<area>: tune-after-evidence items → parked-follow-ons.md § <area>` so the prompt-to-load stays visible at boot; the archive-executor MUST NOT leave non-blocking shipped-change bullets in `open-questions.md`."
- [x] 1.3 In the same section, add a new short paragraph (immediately after the open-questions.md horizon-split paragraph) preceded by the marker line `<!-- CANONICAL: decisions-entry-format — cite, do not restate -->` with text: "**decisions.md entry rule:** Every new `ai-docs/decisions.md` entry (both repos) carries `**Date:** YYYY-MM-DD` and `**Status:** ACTIVE`; change-record entries (`fix-*`/`add-*`/`tune-*`) are capped at ≤300 words, with full rationale in `openspec/changes/archive/<name>/`. No retroactive backfill of existing entries is required."

## 2. P3 — Enforce the bounds in the archive-executor (scaffold)

- [x] 2.1 Augment the existing reconcile subsections in BOTH `/home/pang/Projects/openspec-scaffold/.claude/agents/archive-executor.md` and `/home/pang/Projects/openspec-scaffold/.opencode/agents/archive-executor.md` (their bodies are byte-identical — apply the SAME edits to both). Each addition **cites** `AGENTS.md §"State, write discipline, and the archive-as-handoff rule"` as the canonical home:
  - In `#### 3a. Reconcile STATUS.md`, add a bullet: "**Bound each retained entry** (per AGENTS.md §\"State, write discipline…\"): each `## Latest change`/`## Prior change` section is a ≤150-word headline summary (what shipped, key verify outcome, where details live). If the source narrative is longer, keep the ≤150-word headline and ensure the full narrative lives in the change archive."
  - In `#### 3b. Reconcile ai-docs/decisions.md`, add a bullet: "Every new entry carries `**Date:** YYYY-MM-DD` and `**Status:** ACTIVE` (per AGENTS.md §\"State…\"); change-record entries (`fix-*`/`add-*`/`tune-*`) are capped at ≤300 words with full rationale in the change archive."
  - In `#### 3c. Reconcile open-questions.md…`, add a bullet: "Reduce each shipped-change section in `open-questions.md` to its **BLOCKING** items plus a one-line pointer stub (`<area>: tune-after-evidence items → parked-follow-ons.md § <area>`); park every non-BLOCKING bullet to `parked-follow-ons.md`. Never leave a non-blocking shipped-change bullet in `open-questions.md` (per AGENTS.md §\"State…\")."
- [x] 2.2 In the `#### 3a` STATUS bullet from 2.1 (identically in both files), append the data-loss safety check: "**before trimming**, confirm the archived `notes.md` holds the salient 'why'; if it is thin, copy that context there first, then trim — never drop 'why' context that exists only in `STATUS.md`."
- [x] 2.3 Verify the two archive-executor files remained byte-identical in shared body after the 2.1–2.2 edits by running `python /home/pang/Projects/openspec-scaffold/scripts/test_executor_body_agreement.py`; confirm it passes (fix any divergence by re-applying the identical block to both).

## 3. P3 — Propagate the scaffold rules to both downstream repos

- [x] 3.1 Run `python /home/pang/Projects/openspec-scaffold/scripts/sync_scaffold.py /home/pang/Projects/psc-monitor` then `python /home/pang/Projects/openspec-scaffold/scripts/sync_scaffold.py /home/pang/Projects/extrends` to propagate the updated AGENTS.md spans + archive-executor files.
- [x] 3.2 For BOTH repos, run `sync_scaffold.py --check <repo>` and `sync_scaffold.py --check-refs <repo>`; all four must be green. If `--check-refs` fails on a missing cited per-repo file (`ai-docs/parked-follow-ons.md`, `ai-docs/archive/status-log.md`, `ai-docs/archive/retired-notes.md`), create an empty stub of that file in the repo, then re-run until green.
- [x] 3.3 Run `python /home/pang/Projects/openspec-scaffold/scripts/test_sync_scaffold.py` and confirm it passes.

## 4. P3 — One-time mechanical fix: extrends STATUS.md over-cap entry

- [x] 4.1 In `/home/pang/Projects/extrends/STATUS.md` the change-entry sections currently exceed the 3-entry cap (any `##` section narrating a shipped change counts; the current-state preamble and `## Immediate next action` are exempt). Count the change-entry sections; move the oldest over-cap change-entry section(s) **verbatim** (no summarization, no edits to their text), oldest-first, to the top of `/home/pang/Projects/extrends/ai-docs/archive/status-log.md` (newest-first, append-only; create the file if it does not exist) until exactly the 3 most-recent change-entry sections remain. Leave those 3 and all structural sections untouched. Do not modify any other state file in this task.

## 5. P2 — psc-monitor: enumerate appendix citations before editing

- [x] 5.1 Run `grep -rn 'AGENTS.md §' /home/pang/Projects/psc-monitor --include=*.md` and filter out paths under `openspec/changes/`, `ai-docs/archive/`, and `docs/reviews/`. Record every surviving citation that targets an appendix section (`Database schema`, `Matcher logic`, `API`, `Core code patterns`, `Repository layout`, `Stack`, `Testing`, `Project reference`, etc.) into this change's `notes.md` under a heading `## psc-monitor citation repoint map`, noting for each the new target file from task 6. (These get repointed in 7.4.)

## 6. P2 — psc-monitor: extract the appendix into on-demand ai-docs files (verbatim copy-move)

Create each file by **verbatim** copy of the named `/home/pang/Projects/psc-monitor/AGENTS.md` sections — copy-move, do NOT rewrite or summarize; preserve every detail (e.g. unique-index names, the full API routes table). Prefix each new file with an H1 title and a one-line "When to load" note.

- [x] 6.1 `/home/pang/Projects/psc-monitor/ai-docs/schema-reference.md` ← sections `## Database schema` + `## Matcher logic` (≈ lines 511–581).
- [x] 6.2 `/home/pang/Projects/psc-monitor/ai-docs/api-reference.md` ← section `## API` (≈ lines 583–611).
- [x] 6.3 `/home/pang/Projects/psc-monitor/ai-docs/repo-layout.md` ← section `## Repository layout` (≈ lines 344–415).
- [x] 6.4 `/home/pang/Projects/psc-monitor/ai-docs/ops-runbook.md` ← sections `## Production domain layout` + `## Setup` + `### .env variables` + `## Daily operations` + `### Loading historical snapshots` (≈ lines 319–325, 417–509), plus the **entire** `## Testing` section verbatim (the load-bearing pitfalls also appear in `## Project context` per 7.1 — that redundancy of critical pitfalls is intended; do not try to filter `## Testing`).
- [x] 6.5 **Audit the remaining appendix sections** (those NOT extracted in 6.1–6.4): `## What is this?`, `## Stack`, `## Dependency source of truth`, `## Core code patterns`, `## Known issues / technical debt`, `## Forward plans`, `## Do not do`. For EACH, decide and record a disposition in this change's `notes.md` (under `## psc-monitor citation repoint map`): (a) genuine constraint/pitfall → promote into `## Project context` (this is the same promotion as 7.1; ensure it lands there); (b) reference content that is the target of any citation recorded in 5.1, OR that is non-trivial domain reference → move it **verbatim** into the most appropriate existing new file from 6.1–6.4, or into a new `ai-docs/project-reference.md` catch-all; (c) pure pointer / duplicates always-loaded content AND not cited by anything in 5.1 → may be dropped. **Outcome gate:** after 6.5, every one of those section headings has a recorded disposition AND every citation recorded in 5.1 has a concrete destination file/section (no citation may map to a section that will be dropped).

## 7. P2 — psc-monitor: slim AGENTS.md

- [x] 7.1 In `/home/pang/Projects/psc-monitor/AGENTS.md`, expand `## Project context` (≈ lines 66–83) by adding these exact load-bearing items as bullets (do not drop any): (a) `_PGConn` translates `?`→`%s`; never use f-strings/interpolation in SQL. (b) psycopg2 auto-deserialises JSONB columns; do NOT call `json.loads()` on query results. (c) Postcodes: normalise both sides with `.upper().replace(" ", "")` before comparing. (d) The `pg_conn` test fixture is `autocommit=True`; `ON COMMIT DROP` on temp tables drops them immediately — use plain `CREATE TEMP TABLE` in tests. (e) `pyproject.toml` is the only dependency manifest. (f) Do not use `sys.path.insert` import hacks.
- [x] 7.2 In `## Project context`, replace the sentence that points to "the `# Project reference` body at the end of this file" with: "Detailed technical reference lives in on-demand `ai-docs/` files (see `## On-demand references`)."
- [x] 7.3 Only after tasks 6.1–6.5 have given every appendix section a disposition: delete the entire appendix — from the `# Project reference (durable …)` H1 (≈ line 296) through end of file (≈ line 707) — and replace it with a `## On-demand references` section (keep the existing `---` separator line immediately before it so sync tail-detection still fires). The section body: a "Load the relevant file when you need it — do not preload." line, then a two-column `| What | File |` table mapping → `ai-docs/schema-reference.md` (full schema + matcher logic), `ai-docs/api-reference.md` (API routes), `ai-docs/repo-layout.md` (directory tree), `ai-docs/ops-runbook.md` (dev setup, .env, daily ops, backfill), `openspec/config.yaml` `context:` (what the product is), `plans/` (forward plans), `plans/open-issues.md` (known issues). If task 6.5 created any additional `ai-docs/` file (e.g. `ai-docs/project-reference.md`), add a row for it too so it stays discoverable at boot.
- [x] 7.4 Repoint every citation recorded in 5.1 to the concrete destination assigned in task 6 / 6.5 (e.g. `AGENTS.md §"Database schema"` → `ai-docs/schema-reference.md`; a citation to a 6.5-moved section → its recorded file; a citation to a section promoted into Project context → `AGENTS.md §"Project context"`). After this, no `AGENTS.md §"<appendix section>"` citation remains anywhere in psc-monitor tracked markdown.

## 8. P2 — psc-monitor: deterministic integrity checks

- [x] 8.1 Confirm all four new files exist and are non-empty (`ai-docs/schema-reference.md`, `ai-docs/api-reference.md`, `ai-docs/repo-layout.md`, `ai-docs/ops-runbook.md`).
- [x] 8.2 Confirm no appendix `##`/`#` section heading still remains in `AGENTS.md` (grep for `# Project reference`, `## Database schema`, `## Matcher logic`, `## API`, `## Repository layout`, `## Stack`, `## What is this?`, `## Daily operations`, `## Setup`, `## Known issues`, `## Forward plans`, `## Do not do`, `## Production domain layout`, `## Testing`, `## Core code patterns`, `## Dependency source of truth` → zero matches in AGENTS.md), and confirm `AGENTS.md` line count dropped substantially toward the extrends model (`wc -l` ≈ 330–360 — an approximate sanity check, not a hard gate).
- [x] 8.3 Run `python /home/pang/Projects/openspec-scaffold/scripts/sync_scaffold.py --check /home/pang/Projects/psc-monitor` and `--check-refs /home/pang/Projects/psc-monitor`; both green.
