## 1. Verify skill — multi-model section rewrite (`.claude/skills/openspec-verify-change/SKILL.md`)

- [x] 1.1 Rewrite the pass-sequence bullets under "The pass sequence depends on which platform you are running on:" (currently two platform bullets, the OpenCode one stale — it claims a flash-only chain): replace with a tier-keyed, platform-uniform sequence per the delta spec — MEDIUM: self-review → `deepseek/deepseek-v4-pro` behavioral verifier pass; COMPLEX: self-review → pro behavioral pass → `deepseek/deepseek-v4-flash` lens pass. State explicitly that the sequence is identical on both platforms and that no pass repeats the behavioral checklist a third time (one-line rationale: the three-repo record showed zero non-trivial unique catches from a same-checklist third pass).
- [x] 1.2 Replace the two duplicate invocation subsections ("Claude Code invocation (two passes)" and "OpenCode invocation (pro + flash, same chain as Claude Code)") with ONE subsection "Invocation (both platforms)" containing: (a) the behavioral pro-pass invocation block (unchanged shape: `timeout -k 15 780 opencode run --dir <repoRoot> --agent openspec-verifier --model deepseek/deepseek-v4-pro --format json "<canonical behavioral verifier prompt>" > /tmp/verify-pro-out.jsonl 2> /tmp/verify-pro-err.log < /dev/null ; echo "EXIT=$?" > /tmp/verify-pro-out.exit`); (b) a COMPLEX-only lens-pass invocation block (same shape, `--model deepseek/deepseek-v4-flash`, prompt = selected canonical lens prompt, output files `/tmp/verify-lens-out.jsonl` / `.log` / `.exit`). Keep the §a/§d harness citations.
- [x] 1.3 Inline the canonical **behavioral verifier prompt** as a fenced block in the skill (content: verify the change per your agent definition's behavioral review — git diff + changed files, full-suite re-run via `scripts/test-cmd` or documented command, real-output eyeball, live smoke for external-API surfaces — and emit the `## Verify Pass — <model-id>` verdict block). Remove BOTH occurrences of the phrase `the fixed verifier prompt from design D5` / `the verifier prompt from design D5` — the skill must no longer point into an archived change's design.md.
- [x] 1.4 Add a "Lens pass (COMPLEX)" subsection with the two canonical **lens prompts** as fenced blocks, matching the delta spec's lens menu verbatim in substance: (a) test-quality/adversarial-oracle lens (default) — for each added/modified test, would it fail if the behavior broke (name the tripping assertion); flag tautological/forced-green asserts, empty test bodies, self-mocking of the module under test, discarded return values/flags, unfrozen clocks; (b) data-scale lens (data-path-dominant changes) — unbounded input domains, full materialization of unbounded queries (`fetchall()` et al.), at-scale-run need or recorded bounded-domain argument. Include: the selection rule (orchestrator selects per change, records one-line rationale in `review-log.md`), the diff-scope rule (lens pass NOT required to re-run the full suite — the pro pass already did; targeted probes allowed), the MEDIUM opt-in sentence, and the detector-handoff sentence (when a corresponding deterministic detector ships, the lens prompt directs the verifier to run and confirm its findings rather than rediscover them).
- [x] 1.5 Update "Gate semantics and recovery" step 2 for the new chain shape: MEDIUM — pro fails → fix → re-run pro (it is the only and last delegated pass); COMPLEX — pro fails → fix → re-run pro then lens; lens fails → fix → re-run lens only. Remove the OpenCode-single-pass parenthetical. Keep the 3-cycle loop bound and the self-review-defect path unchanged.
- [x] 1.6 Sweep the rest of the SKILL for chain vocabulary: step 9's text, step 16's multi-model recording bullet, and step 17 field 3's attribution list — replace "(self-review, pro pass, or flash pass)" with "(self-review, pro pass, or lens pass)" and require, for a lens pass, recording the selected lens + one-line rationale. Also update the tier-applicability sentence in "Multi-model passes" if it references the old chain, and the line-46 sentence "It runs the same behavioral review you performed in the self-review" to say the verifier runs the review its invocation prompt specifies (behavioral by default; lens for the lens pass).

## 2. Delegation harness (`.claude/skills/_shared/delegation-harness.md`)

- [x] 2.1 Update the §e budget-table rows for the verifier: rename `verifier (pro pass)` → `verifier (behavioral pass, pro)` and `verifier (flash pass)` → `verifier (lens pass, flash — COMPLEX only)`, keeping both budgets exactly `780` / `-k 15` (the `scaffold_lint.py` budget-agreement check byte-compares embedded timeout pairs against this table — budgets must not change).
- [x] 2.2 Update the harness prose that says the verifier runs "pro + flash" on both platforms (two sites: the §-intro describing the verifier chain and the later restatement) to the tier-keyed wording: behavioral pro pass (MEDIUM and COMPLEX) + lens flash pass (COMPLEX only), identical on both platforms.

## 3. AGENTS.md (scaffold-managed shared span)

- [x] 3.1 Rewrite the `openspec-verifier` role bullet (the "## Roles" section): multi-model verification is tier-keyed and platform-uniform — SMALL: single flash pass outside the verify skill (UNCHANGED); MEDIUM: verify skill runs self → pro behavioral; COMPLEX: self → pro behavioral → flash lens pass (a different fixed checklist — test-quality or data-scale — selected by the orchestrator and recorded). Replace the "pass chain is identical on both platforms: … self → pro → flash" sentences and the "two invocations: --model … pro then … flash" parenthetical accordingly. Do NOT alter the SMALL-tier rules in "## Change tiers", EXCEPT one surgical parenthetical fix: the SMALL bullet's "(same invocation shape as in the verify skill's flash pass)" would dangle once the flash behavioral pass is removed — change it to "(same invocation shape as the verify skill's behavioral verifier pass, run at the `deepseek/deepseek-v4-flash` model tier)". The SMALL pass stays a BEHAVIORAL review on flash — it is NOT a lens pass.
- [x] 3.2 Sweep the remaining AGENTS.md chain mentions — the workflow step 4 description ("independent multi-model verification passes"), the MEDIUM/COMPLEX tier bullets ("Runs the verify skill (including its multi-model passes…)"), and item (2) of "After reading this file" — updating any that name the old self→pro→flash shape; leave wording that is already chain-shape-neutral untouched.

## 4. Verifier agent (`.opencode/agents/openspec-verifier.md`)

- [x] 4.1 Generalize the agent body: after the opening role paragraph, state that the verifier executes the fixed review prompt supplied by the orchestrator's invocation — the behavioral review in "## Your Review" is the DEFAULT, used whenever the prompt does not specify a lens; when the invocation supplies a lens prompt (test-quality or data-scale), execute that checklist instead, diff-scoped, without a mandatory full-suite re-run. Keep "## Your Review" itself, the Delegating-exploration, Data-safety, Prohibitions, and Verdict-Format sections unchanged (the verdict block is shared by both pass types — state that explicitly in the Verdict Format section).
- [x] 4.2 Update the frontmatter `description:` to cover both pass types (behavioral verify review by default; lens review when the invocation specifies one; read-only; never fixes). Do not touch the `permission:` block.

## 5. Repo docs that describe the chain

- [x] 5.1 Check `knowledge/lessons.md` for any assertion of the verify chain SHAPE ("self→pro→flash" or a three-same-pass description) and update it to the new tier-keyed shape (MEDIUM self+pro; COMPLEX self+pro+lens) if found. Review round 1 found NO such line — the only verifier mention (a singular "deepseek-v4-pro openspec-verifier pass" technique reference) remains correct post-change; if that is still the case, make no edit and check this task off as verified-no-op. Note: `knowledge/STATUS.md` also contains historical chain vocabulary ("self-review + pro + flash … READY") — that is a shipped-change historical record reconciled at ARCHIVE, deliberately NOT an apply-time edit; do not touch it.

## 6. Green gate

- [x] 6.1 Run `bash scripts/check.sh` (ruff + format + full pytest suite, including the `scaffold_lint` SEAL and its budget-agreement check) and fix any failures introduced by the edits above until the gate is green.
